---
- hosts: test-api
  become: yes

  vars:
    install_npm_user: root
    npm_config_prefix: "/usr"
    npm_global_packages:
      - forever
    firewall_allowed_tcp_ports:
      - "22"
      - "8080"

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600

  roles:
    - geerlingguy.firewall
    - geerlingguy.git